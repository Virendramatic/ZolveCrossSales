{
  "version": 2,
  "buildCommand": "npm --prefix backend install && npm --prefix backend run build",
  "functions": {
    "vercel/functions/api.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}

