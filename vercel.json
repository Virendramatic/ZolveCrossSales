{
  "version": 2,
  "buildCommand": "cd backend && npm install && NODE_OPTIONS=--no-warnings npm run build",
  "outputDirectory": "public",
  "functions": {
    "api/index.js": {
      "runtime": "@vercel/node@3.0.0"
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api"
    },
    {
      "src": "/health",
      "dest": "/api"
    },
    {
      "src": "/(.*)",
      "dest": "/api"
    }
  ]
}
