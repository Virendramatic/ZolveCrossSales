{
  "version": 2,
  "buildCommand": "npm --prefix backend install && npm --prefix backend run build",
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}

